<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Board">
	<insert id="save" parameterType="board">
		insert into board2 (title, contents, writer, pass) values(#{title},
		#{contents}, #{writer}, #{pass})
	</insert>

	<select id="findAll" resultType="board">
		select * from board2
	</select>

	<select id="findById" parameterType="Long" resultType="board">
		select id, title, writer, pass,contents
		from board2 where id =#{id}
	</select>

	<update id="update">
		update board2 set title=#{title}, contents=#{contents} where id = #{id}
	</update>

	<delete id="delete" parameterType="Long">
		delete from board2 where id=#{id}
	</delete>
</mapper>